version: '3.8'

services:
  backend:
    build: ./backend
    container_name: okr_backend
    ports:
      - "25000:25000"
    environment:
      - JAVA_OPTS=-Djava.net.preferIPv4Stack=true
      - SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres
      - SPRING_DATASOURCE_USERNAME=postgres.nvuuberydtmanycnhskk
      - SPRING_DATASOURCE_PASSWORD=Xl2sgBJ3DBZ0pmTI
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect
    restart: always

  frontend:
    build: ./frontend
    container_name: okr_frontend
    ports:
      - "8080:8080"
    environment:
      - WATCHPACK_POLLING=true
      - BACKEND_PRIVATE_IP=172.31.136.143
    restart: always
